{
  "name": "rubezh-strazh-pass-printer",
  "private": true,
  "version": "2.3.3",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc && esbuild extension-ts/content.ts --bundle --format=iife --target=chrome120 --outfile=src/content.js",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "typescript": "^5.9.2"
  }
}
