import{RubezhAdapter}from'./adapter.js';const a=new RubezhAdapter();const ID='rubezh-pass-print-button';
async function inject(){if(!a.isEmployeePage()||document.getElementById(ID))return;const b=document.createElement('button');b.id=ID;b.type='button';b.textContent='Печать пропуска';Object.assign(b.style,{margin:'6px',padding:'8px 14px',border:'1px solid #2374a6',borderRadius:'3px',background:'#fff',color:'#2374a6',cursor:'pointer',font:'inherit'});b.addEventListener('click',async()=>chrome.runtime.sendMessage({type:'OPEN_PRINT_DIALOG',employee:await a.getEmployeeData()}));const anchor=a.getActionAnchor();(anchor?.parentElement||document.querySelector('employee-view')||document.body).append(b)}
new MutationObserver(()=>void inject()).observe(document.documentElement,{childList:true,subtree:true});void inject();chrome.runtime.onMessage.addListener((m:any,_s:any,reply:(v:any)=>void)=>{if(m.type==='GET_EMPLOYEE')a.getEmployeeData().then(employee=>reply({ok:true,employee})).catch(error=>reply({ok:false,error:String(error)}));return true});
